# Legacy packaging files

These files were used by a nested `light_of_the_seven/` project layout and are now obsolete. The canonical packaging configuration is at the repository root: `pyproject.toml` (hatchling).

- `setup.py` — legacy setuptools setup for nested layout
- `pyproject_nested.toml` — nested project config (setuptools, different entry point)
